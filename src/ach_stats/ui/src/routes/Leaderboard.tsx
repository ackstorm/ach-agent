// Placeholder — replaced by the real leaderboard page in Task 10 (needs the
// contract types + useLeaderboard hook from Task 9 first).
export default function Leaderboard() {
  return <div className="p-8 text-muted-foreground">ach-stats</div>;
}
