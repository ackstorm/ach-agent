apiVersion: v1
kind: Service
metadata:
  name: {{ include "ach-agent.fullname" . }}
  labels:
    {{- include "ach-agent.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  selector:
    {{- include "ach-agent.selectorLabels" . | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.service.port }}
      targetPort: http
